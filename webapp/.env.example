# Base URL for the FastAPI backend. Leave empty in dev to use the Vite proxy
# (see vite.config.js, which forwards /api to http://localhost:8000).
# Set this to the deployed backend origin in production, e.g. https://api.example.com
VITE_API_BASE_URL=
